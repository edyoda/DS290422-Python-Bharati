lst = [9,8,1,2,10,5,3,1,1,1,1]
print("Orginal List : ",lst)

# print(len(lst))

# lst.append(10) # add values in existing list and it will add one value at a time at the end

# lst.insert(3,11) # index,value # it will insert the value in the existing list as per the index mentioned

# lst.extend([1,2,4]) # it will add multiple values at the end

# lst.remove(5) #value # will remove the first occurrence of the value

# lst.pop() # by default removes the last value

# lst.pop(3) # it will delete the value on the bases of index passed as argument

# lst.reverse() # it is used to return the reverse of list

# lst1 = lst.copy()
# print(lst1)

# lst.sort() # by default it will sort the list in ascending order

# lst.sort(reverse=1)

# lst.clear() # it will clear all the elements and will return empty list

# store = lst.pop()
# print(store)

counter = lst.count(1)
print(counter)

# indexing = lst.index(10)
# print(indexing)

# another way of creating list (list constructor) 
# also it used for type casting (meaning if one datatype needs to be converted into list)
# lst = list([1,2,3,4,5]) 











print("Modified List : ",lst)

"""
if maam_speed == "1x":
    print("normal speed sbb aa raaha h ");
elif maam_speed == "5x":
    print("Python : bheti hawa sa thaa vo chuke chala gya dimag ko ")
else:
    print("kaha gya usse dhundoo.....")
"""