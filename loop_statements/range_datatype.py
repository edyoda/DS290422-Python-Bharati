# range datatype - it expects for 3 parameters
# - it is used to define the range

data = range(1,500,4) # start, stop/end, step 
print(data)

data1 = range(1,500) # start, stop, step = 1 (bydefault value for step)
print(data1)

data2 = range(500) # end    start = 0, setp = 1
print(data2)